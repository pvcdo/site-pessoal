{{#each posts}}
    {{> post}} <!-- o uso do sinal '>' indica que cada objeto do array posts será tratado na página post.handlebars do diretório ./partials. O {{this}} do each será tratado na página post -->
{{/each}}