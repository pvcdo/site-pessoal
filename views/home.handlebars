<h1>Olá Handlebars!</h1>

{{#if auth}}
    <p>
       {{admin.name}} está logado (o auth está como true no index.js)
    </p>
    <a href="/dashboard">Dashboard</a>

{{else}}
    <p>
       {{admin.name}} não está logado (o auth está como false no index.js)
    </p>
{{/if}}